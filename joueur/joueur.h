#ifndef MAIN
#define MAIN

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#endif

#include "../structure.h"

char configPlayers(Move *move, char rep[3]);
char lancerPartie(Move *move, char rep[3]);